#include <stdio.h>
#include <stdbool.h>
#define MAX 100

struct Person{
    char name[MAX];
    int age;
    int yob; // year of birth
    int yod; // year of death
    int yoa; // year of arrival
    bool isTimeTraveler;
};

int main(){
    
}